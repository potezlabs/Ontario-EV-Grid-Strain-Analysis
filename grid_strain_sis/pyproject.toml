[project]
name = "grid_strain_analysis"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.48.0",
    "snowflake-snowpark-python",
    "pydeck",
    "pandas",
    "numpy",
]
